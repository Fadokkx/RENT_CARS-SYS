-- Active: 1715997549864@@localhost@5432@cars
CREATE TABLE customers ();
ALTER TABLE customers ADD COLUMN customer_id SERIAL PRIMARY KEY;
ALTER TABLE customers ADD COLUMN name VARCHAR(50) NOT NULL;
ALTER TABLE customers ADD COLUMN birth_date DATE NOT NULL;

ALTER TABLE costumers ADD FOREIGN KEY 

CREATE TABLE CARS (
    car_id SERIAL PRIMARY KEY,
    year_model INTEGER NOT NULL,
    make VARCHAR(50) NOT NULL,
    model VARCHAR(40) NOT NULL, 
    category VARCHAR (20) NOT NULL,
    plate VARCHAR(15) NOT NULL);

    INSERT INTO cars VALUES (default, 2012, 'MITSUBISHI', 'i-MiEV', 'SUBCOMPACT', 'ABC123');